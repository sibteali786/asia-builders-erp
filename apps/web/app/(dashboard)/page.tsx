import { DashboardHome } from "@/components/dashboard/dashboard-home";

// Server Component — composes the client dashboard shell (role + filters + sections).
export default function DashboardPage() {
  return <DashboardHome />;
}
