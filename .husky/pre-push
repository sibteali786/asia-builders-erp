echo "🧪 Running full test suite before push..."

# Run all tests (unit + integration + e2e)
pnpm run test || {
  echo "❌ Tests failed. Push aborted."
  echo "💡 Fix failing tests or push with --no-verify (not recommended)"
  exit 1
}

echo "✅ All tests passed! Safe to push."